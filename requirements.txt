# Core — needed to run `python run_demo.py` (offline, no keys)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# API + frontend backend
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Automation (nightly scheduler + alerts) and BI export
apscheduler>=3.10
openpyxl>=3.1

# Tests
pytest>=8.0

# ---- Optional extras (uncomment as needed) ----
# Live market data (python run_demo.py --live):
# yfinance>=0.2.40
#
# Postgres backend (set DATABASE_URL=postgresql://...):
# psycopg[binary]>=3.1
#
# Redis cache (set REDIS_URL=redis://...):
# redis>=5.0
#
# Nicer LLM prose + LLM planner (set ANTHROPIC_API_KEY):
# anthropic>=0.40
#
# C++ risk engine build (pip install ./risk/cpp):
# pybind11>=2.12
#
# Deep-learning volatility model (models/lstm_vol.py):
# torch>=2.2
